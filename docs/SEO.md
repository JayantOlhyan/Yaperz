# SEO & Structured Metadata Strategy

## Standard Metadata
- Next.js Metadata API used for title, description, and OpenGraph tags.
- Dynamic route segment metadata builders in `src/lib/seo.ts`.

## Structured Data (JSON-LD)
- `Product`: Rich snippets for Google Shopping with price and availability.
- `BreadcrumbList`: Navigation hierarchy representation in SERP.
